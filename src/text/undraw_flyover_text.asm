
UNDRAW_FLYOVER_TEXT:
	REP #PROC_FLAGS::ACCUM8 | PROC_FLAGS::INDEX8 | PROC_FLAGS::CARRY
.IF .DEFINED(JPN)
	STACK_RESERVE_VARS
	STACK_RESERVE_INT32
	END_STACK_VARS
.ENDIF
	LDY #$6000
	LDX #$7C00
	LDA #BG_TILEMAP_SIZE::NORMAL
	JSL SET_BG3_VRAM_LOCATION
	JSL UPLOAD_FULL_TEXT_TILEMAP
	JSL PREPARE_WINDOW_GRAPHICS
.IF .DEFINED(JPN)
	COPY_TO_VRAM3 BUFFER, $6000, $3800, @VIRTUAL00
.ELSE
	LDA #$0002
	JSL LOAD_WINDOW_GRAPHICS
.ENDIF
	JSL LOAD_TEXT_PALETTE
	SEP #PROC_FLAGS::ACCUM8
	LDA #PALETTE_UPLOAD::FULL
	STA PALETTE_UPLOAD_MODE
	REP #PROC_FLAGS::ACCUM8
.IF .DEFINED(JPN)
	PLD
.ENDIF
	RTL
